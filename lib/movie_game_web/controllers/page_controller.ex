defmodule MovieGameWeb.PageController do
  use MovieGameWeb, :controller

  def index(conn, _params) do
    render conn
  end

  def login(conn, _params) do
    render conn
  end

  def signup(conn, _params) do
    render conn
  end
end
